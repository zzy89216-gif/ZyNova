# 更新日志 (Changelog)

本项目所有值得注意的变更都会记录在此文件中。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/)。

## [26.1.0] - 2026-09-25

这一版本的目标是：**进一步脱离 ZalithLauncher2 的遗留逻辑，建立 ZyNova 自己的资源管理、下载、主页与 UI 基础。**

核心原则：**Context First. Less Steps.**（能自动判断就不让用户选择，能一步完成就不拆成两步，已有上下文就直接使用，没有必要的按钮直接删除。）

### 新增

#### 统一资源管理核心（Resource Management Core）
- 新增统一资源模型：`Resource`、`ResourceVersion`、`ResourceFile`、`ResourceDependency`、`ResourceType`
  - Mod、资源包、光影、存档等资源统一使用同一套类型
  - 统一的统一流程：**搜索 → 资源详情 → 版本匹配 → 文件选择 → 下载 → 校验 → 安装**
- 新增 `ResourceManager` 作为统一流程入口，UI 不再各自实现下载与安装逻辑
- 新增 `ResourceInstallManager`：统一负责版本匹配、文件选择、递归解析必需前置依赖、安装与状态判定

#### 资源来源（Resource Provider）
- 新增 `ResourceProvider` 统一接口与 `ResourceProviders` 注册表
- Modrinth、CurseForge 作为独立来源实现，与 UI 完全解耦
- 上层只依赖统一接口，以后新增资源来源只需实现接口并注册，不需要重写资源系统
- 未加入 BBSMC

#### 统一下载管理器（Download Manager）
- 新增 `ResourceDownloadManager`，统一负责：
  - 下载队列与并发控制
  - 下载进度聚合
  - 断点续传与失败重试
  - 取消下载
  - 文件校验（sha1）
  - 临时文件清理
  - 下载完成后的安装触发
- Mod、资源包、光影、存档全部使用同一个下载入口，各资源页面不再自己实现下载器

#### 极简资源安装
- 从「版本设置 → Mods / 资源包 / 光影 / 存档」进入资源页面时，系统已经知道当前实例、Minecraft 版本、加载器与资源目录
  - 点击卡片上的下载按钮后直接：**检查兼容性 → 选择兼容文件 → 下载 → 校验 → 安装**
  - 不再重复要求用户选择 Minecraft 版本、实例或安装位置
- 资源版本列表新增**「已安装」状态显示**
- 从主界面进入的资源中心只负责浏览与管理，**不再提供一键安装入口**

#### 卡片式主页
- 新增卡片式主页，自动识别并展示：
  - 最近使用的 Minecraft 版本 → 点击**启动**
  - 本地世界 → 点击**进入**
  - 已保存的服务器 → 点击**加入**
- 设置中可选择主页类型：**默认主页 / 卡片主页 / 自定义主页**
- 新增 `HomeDataProvider` 作为主页统一数据访问接口，卡片主页与自定义主页共用
- 数据**按需加载**：只有真正进入主页时才读取，且限量扫描，启动器启动时不做全盘扫描

#### 玻璃效果（Glass UI）三档
- 液态玻璃效果由「开 / 关」改为三档：**关闭 / 标准 / 增强**
- **默认关闭**（性能优先）
  - 标准档：静态高光，**不启动任何持续动画**
  - 增强档：高光缓慢流动，效果最好但开销更高
- 避免高开销实时模糊、大量透明层叠加与持续高负载动画

#### Minecraft 26.4 Snapshot 1 Vulkan 适配
- 新增 `VulkanRequirement` 要求档案，把「Minecraft 需要什么」与「怎样检测」分离
- 检测依据改为设备**实际枚举出的 Vulkan 能力**，而不是设备在 `PackageManager` 中声明的支持情况
- 检测结果明确区分三种状态：**可用 / 不可用 / 检测失败**
- 检测内容涵盖：Vulkan 是否可用、Vulkan API 版本、驱动是否正常、必要扩展与功能、GPU / 渲染器信息、检测失败原因
- 不可用时给出具体原因：无可用 Vulkan 设备 / API 版本过低 / 缺少扩展 / 缺少功能 / 驱动异常
- 检测结果对话框新增**「重新检测」**按钮
- 检测失败不再被缓存为「不支持」，避免之后不再重新检测

#### 关于页面
- 开发者署名改为 **zzy**，补充 GitHub 仓库与 **Discord 服务器**入口
- 新增主页类型与玻璃效果档位的界面文案

### 变更
- 版本号更新为 **26.1.0**
- 极简安装的底层实现改为调用统一资源核心
- 主页类型设置项扩展为三种主页
- 玻璃效果设置项由开关改为三档单选

### 移除
- **移除添加账号界面的正版（微软）登录入口**，同时清理对应的 UI、点击逻辑、状态、无用资源与依赖
  - 保留已有微软账号的会话续期能力，确保旧用户升级后仍可正常启动游戏
- **彻底移除 ZalithLauncher2（ZL2）更新链**：
  - 移除 ZL2 更新检查、更新提示、更新弹窗、更新 URL、对应 UI、检查逻辑与状态
  - 移除上游版本信息文件（GitHub Contents）解析与 Base64 内容解码
  - 移除网盘分发链路
- 移除资源中心的一键安装入口
- 移除指向已不存在的上游 Weblate 项目的入口
- 移除基于设备声明的 Vulkan 支持判断工具
- 清理 13 个语言文件中已无引用的微软登录提示文案
- 清理多处无用 import 与死代码

### 修复
- 修复关于页面署名错误：原「ZalithLauncher2 作者」卡片被错误显示为 ZyNova 的作者
- 修复旧的液态玻璃布尔开关与新档位设置之间的配置兼容问题

### 兼容性
- 旧配置保持兼容，旧用户升级后仍可正常启动：
  - 数据库中已有的微软账号仍可读取、刷新并用于启动游戏
  - 旧的「液态玻璃」开关会自动迁移为新的玻璃效果档位
  - 旧的「已忽略更新版本号」配置项保留定义，避免读取异常
- 包名仍为 `com.zynova.launcher`，可与 ZalithLauncher2 共存

## [2.5.1] - 2026-09-06

### 修复
- 修复一键安装时不自动下载前置依赖（前置模组）的问题
  - 原因是忽略了 Modrinth 依赖中作者指定的精确版本 `version_id`
  - 现在会优先使用作者指定的精确前置版本，确保前置依赖正确下载安装

## [2.5] - 2026-09-06

### 新增
- 液态玻璃效果（毛玻璃之上叠加动态流动高光，可在设置里开关）
- 一键安装模组/光影（自动选适配版本 + 自动装 REQUIRED 前置依赖）
- 应用改名 ZyNova Launcher（包名 com.zynova.launcher）

### 合规
- README 声明基于 ZalithLauncher2 且为非官方修改版
- 应用内「关于」页面补充非官方声明 + GPL-3.0 许可证入口
- 项目/社区链接指向 ZyNova 仓库

---

## 说明

- 本项目基于 [ZalithLauncher2](https://github.com/ZalithLauncher/ZalithLauncher2)，遵循 **GPL-3.0** 许可证
- ZyNova 是 ZalithLauncher2 的非官方修改版本，并非官方发布
- 关于 26.1.0 之后的发展方向，见 [README](README.md) 中的 Roadmap 章节
